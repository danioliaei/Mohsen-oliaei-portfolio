version: '3.8'

services:
  portfolio-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80" # nginx serves on 80 inside the container; change the first 3000 for a different host port
    restart: unless-stopped
